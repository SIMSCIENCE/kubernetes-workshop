apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: helloworld-pvc
  namespace: svdh
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi